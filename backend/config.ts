const config: Object = {
    //can be stored in env
    SQLDB: {
        host: "localhost",
        user: "root",
        password: "password",
        database: "jobportal",
        connectTimeout: 60000
    }
}
module.exports = config;
